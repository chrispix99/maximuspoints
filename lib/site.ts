/**
 * Canonical public origin for maximusPoints.
 *
 * The app is served on several Vercel aliases, but the team-scoped
 * `*.vercel.app` automatic aliases carry Vercel's `X-Robots-Tag: noindex`
 * header, so Google can never index them. Everything SEO-facing
 * (sitemap, robots, canonical tags) must point at the custom domain.
 */
export const SITE_URL = "https://pointmaximus.com";
